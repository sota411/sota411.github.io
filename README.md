### 自身のポートフォリオを公開しています

## 記事運用（Markdown）

1. `npm run add-article` を実行し、`action` を選びます（`1:追加 / 2:削除 / 3:一覧表示`）。
2. 作成された `articles/*.md` に本文を書きます。
3. 本文やメタ情報を更新した場合は `npm run build-articles` を実行します。

### index.json の必須項目

- `slug`（英小文字・数字・ハイフン）
- `title`
- `date`（`YYYY-MM-DD`）
- `summary`
- `source`（Markdownファイル名）

## 記事運用の詳細手順

追加・更新・削除を含む詳細手順は次を参照してください。  
`docs/articles-operations.md`
